package com.idbat.mobile.ui.theme

import androidx.compose.ui.graphics.Color

// ── Marque Veolia ─────────────────────────────────────────────────────────────
val VeoliaPrincipal   = Color(0xFFD96A56) // Corail principal — interactions, boutons, tabs
val VeoliaCoral       = Color(0xFFF27059) // Corail lumineux — dégradés décoratifs, accents
val VeoliaCoralLight  = Color(0xFFFF9A82) // Corail très clair — fin de dégradé photo
val VeoliaGradientTop = Color(0xFFF8A282) // Haut du dégradé écran login
val VeoliaGradientBot = Color(0xFFE96D71) // Bas du dégradé écran login
val VeoliaCoralText   = Color(0xFFEA6E72) // Texte corail sur fond clair (bouton connexion)

// ── Sémantiques ───────────────────────────────────────────────────────────────
val VeoliaSuccess     = Color(0xFF4CAF50) // Vert succès — badges actifs, icônes NFC OK
val VeoliaSuccessDark = Color(0xFF007F2D) // Vert foncé — badge StatusBadge "Envoi/Réception"
val VeoliaWarning     = Color(0xFFFF9800) // Orange avertissement — CRC invalide
val VeoliaErrorDark   = Color(0xFFC60000) // Rouge erreur — badge StatusBadge en échec
val VeoliaAlertOrange = Color(0xFFF57C00) // Orange alerte — icône ValidationErrorDialog

// ── Neutres ───────────────────────────────────────────────────────────────────
val VeoliaInk         = Color(0xFF1C1C1E) // Encre sombre — trait de signature
val VeoliaDisabled    = Color(0xFFE0E0E0) // Éléments inactifs / désactivés
val VeoliaSubtle      = Color(0xFFDDDDDD) // Bordures et séparateurs discrets
val VeoliaPlaceholder = Color(0xFFBBBBBB) // Texte placeholder zone de dessin
val VeoliaDrawingBg   = Color(0xFFFAFAFA) // Fond zone de dessin signature

// ── Palette de base ───────────────────────────────────────────────────────────
val VeoliaGray        = Color(0xFF333333)
val VeoliaLightGray   = Color(0xFFF5F5F5)
val White             = Color.White
val Black             = Color.Black
